
export { default as React, useState, useRef, useEffect, useContext, useMemo, useCallback, useReducer, useLayoutEffect, createContext, forwardRef, RefObject } from 'react';
export { default as Modal } from 'react-native-modal';

export { SafeAreaProvider, SafeAreaView } from 'react-native-safe-area-context';
export { Slot, router, useRouter }        from 'expo-router';

export * from 'react-native';

import * as Animatable from 'react-native-animatable';
export { Animatable };

export { LinearGradient }   from 'expo-linear-gradient';
export * as SplashScreen    from 'expo-splash-screen'

import * as Font from 'expo-font';
export { Font };