generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE")
}

model users {
  id        Int      @id @default(autoincrement())
  email     String   
  name      String?
  age       String?
  createdAt DateTime @default(now())
}
